---
phase: 10-drift-accuracy
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/test_drift_detector.py
autonomous: true
requirements:
  - DRFT-01
  - DRFT-02
must_haves:
  truths:
    - "A weekend-only behavior change triggers a drift alert without being diluted by weekday data"
    - "Recent days contribute more to the baseline than data from 60+ days ago"
    - "Entities with insufficient day-type-split data still receive drift detection via fallback"
    - "Full test suite passes after phase 10 implementation"
  artifacts:
    - path: "tests/test_drift_detector.py"
      provides: "End-to-end scenario tests for DRFT-01 and DRFT-02 success criteria"
      contains: "TestWeekendIsolationScenario"
  key_links:
    - from: "tests/test_drift_detector.py TestWeekendIsolationScenario"
      to: "drift_detector.DriftDetector.check()"
      via: "Saturday check_date ensures day_type='weekend' path is exercised"
      pattern: "weekday.*>=.*5|saturday|weekend"
---

<objective>
Write end-to-end scenario tests that validate the four phase success criteria as observable behaviors, confirm `make test` passes cleanly, and confirm `make lint` is clean.

Purpose: The unit tests in Plan 10-01 verify the helper methods in isolation. This plan verifies the complete success criteria from the phase requirements — including the weekend isolation scenario and recency weighting operating together at the CUSUM level.

Output: New test class TestWeekendIsolationScenario and TestRecencyWeightingScenario in tests/test_drift_detector.py; clean `make test` and `make lint` results.
</objective>

<execution_context>
@/Users/abourne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abourne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-drift-accuracy/10-01-SUMMARY.md
@custom_components/behaviour_monitor/drift_detector.py
@tests/test_drift_detector.py

<interfaces>
<!-- Use the helper from test_drift_detector.py already established: -->

```python
def _build_routine_with_history(
    entity_id: str, daily_rates: list[int], base_date: date | None = None
) -> EntityRoutine:
    """Events placed at sequential hours; base_date defaults to 2024-01-15 (Monday)."""
```

Note: 2024-01-15 is a Monday (weekday). To get a Saturday base_date use 2024-01-20.

Weekday check: today.weekday() < 5
Weekend check: today.weekday() >= 5

Key dates for test construction:
- 2024-01-15 = Monday (weekday=0)
- 2024-01-20 = Saturday (weekday=5)
- 2024-01-21 = Sunday (weekday=6)

To build a routine with mixed weekday/weekend history, use base_date on a known
day and iterate backwards — each day_offset step goes back one calendar day,
cycling through weekdays and weekends naturally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestWeekendIsolationScenario and TestRecencyWeightingScenario tests</name>
  <files>tests/test_drift_detector.py</files>
  <action>
    Add two new test classes at the end of tests/test_drift_detector.py:

    **TestWeekendIsolationScenario** — validates DRFT-01 success criteria 1 and 3:

    test_weekend_shift_alerts_without_weekday_dilution:
    - Build a routine with a history starting on Saturday 2024-01-20, going back 28 days.
    - 20 of those days are weekdays with rate=5 events/day (stable).
    - 8 of those days are weekends with rate=5 events/day (stable).
    - "Today" is Saturday 2024-01-27. Today's rate = 20 events (4x the weekend baseline).
    - Record the 20 today events directly on the routine (Saturday at 2024-01-27).
    - Run detector.check() for each of 5 simulated Saturdays with the shifted rate.
    - Assert that an alert fires within 5 Saturday checks (one check per Saturday).
    - Assert the alert direction == "increase".
    - Rationale: with 8 weekend baseline days (>= MIN_EVIDENCE_DAYS=3), the day-type split activates. The 20 weekday days do NOT dilute the weekend baseline, so the 4x shift is detectable.

    test_fallback_still_detects_drift_with_few_weekend_days:
    - Build a routine with 14 weekdays at rate=5 but only 2 weekend days at rate=5 (< MIN_EVIDENCE_DAYS).
    - "Today" is Saturday with rate=20.
    - Run for up to 8 simulated Saturdays.
    - Assert that an alert eventually fires (fallback combined pool still catches the shift).
    - This validates success criterion 4: graceful fallback preserves detection.

    **TestRecencyWeightingScenario** — validates DRFT-02 success criterion 2:

    test_recency_weighted_baseline_reflects_recent_data:
    - Build a routine where the first 21 days of history have rate=5 (old data, 3+ weeks ago)
      and the most recent 7 days have rate=15 (recent shift in baseline days).
    - "Today" is stable at rate=5 (matches the old baseline, not the recent one).
    - With recency weighting, the recent rate=15 data dominates the baseline, making today's
      rate=5 appear LOW relative to the weighted baseline — this should produce a downward drift.
    - Run for up to 8 simulated days.
    - Assert that an alert fires with direction == "decrease".
    - Without recency weighting, the 21-day old data at rate=5 would dominate and no drift
      would be detected. With decay weighting, the recent rate=15 wins, causing the drift.
    - Note: Use weekday dates throughout to avoid day-type split effects.

    Implementation notes:
    - Use `datetime(year, month, day, hour, minute, 0, tzinfo=timezone.utc)` for all timestamps.
    - Build history by calling routine.record() directly for fine control over which calendar
      dates events land on — the _build_routine_with_history helper can be used for weekday-only
      scenarios but manual construction gives more control for mixed weekday/weekend scenarios.
    - For the weekend isolation test, build history manually:
      * For each of the 28 days back from 2024-01-19 (the day before "first Saturday today"):
        record 5 events; the slot index will naturally be weekday or weekend based on the date.
      * Then for each simulated Saturday "today", record 20 events on that Saturday.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && source venv/bin/activate && python -m pytest tests/test_drift_detector.py::TestWeekendIsolationScenario tests/test_drift_detector.py::TestRecencyWeightingScenario -v 2>&1 | tail -30</automated>
  </verify>
  <done>TestWeekendIsolationScenario and TestRecencyWeightingScenario all pass. No existing tests broken.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite and lint validation</name>
  <files>tests/test_drift_detector.py</files>
  <action>
    Run the complete test suite and linter. Fix any issues found.

    1. Run `make test` — all tests must pass.
    2. Run `make lint` — zero errors permitted.

    Common issues to look for and fix:
    - Type annotation gaps on new test helpers (add `-> None` return types).
    - Import of `timedelta` at module level if used in new tests (already imported in the file — verify before adding duplicate imports).
    - Any ruff violations in the new test code (line length, unused imports).

    If `make lint` fails due to ruff line-length violations in test file, wrap long lines using implicit string continuation or backslash. Black line length is 88 chars.

    Do NOT modify drift_detector.py in this task — only fix test code style issues.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && source venv/bin/activate && make test && make lint 2>&1 | tail -20</automated>
  </verify>
  <done>`make test` exits 0 with all tests passing. `make lint` exits 0 with zero warnings or errors.</done>
</task>

</tasks>

<verification>
Final confirmation of all four phase success criteria:

1. Weekday drift baseline uses only weekday observations: verified by TestDayTypeBaseline + TestWeekendIsolationScenario
2. Recent days contribute more via exponential decay: verified by TestDecayWeighting + TestRecencyWeightingScenario
3. Weekend-only behavior change triggers alert without weekday dilution: verified by test_weekend_shift_alerts_without_weekday_dilution
4. Graceful fallback for insufficient day-type data: verified by test_fallback_still_detects_drift_with_few_weekend_days

```bash
cd /Users/abourne/Documents/source/behaviour-monitor && source venv/bin/activate && make test 2>&1 | tail -10
```
</verification>

<success_criteria>
- TestWeekendIsolationScenario.test_weekend_shift_alerts_without_weekday_dilution passes
- TestWeekendIsolationScenario.test_fallback_still_detects_drift_with_few_weekend_days passes
- TestRecencyWeightingScenario.test_recency_weighted_baseline_reflects_recent_data passes
- `make test` exits 0 (all tests pass)
- `make lint` exits 0 (no lint errors)
</success_criteria>

<output>
After completion, create `.planning/phases/10-drift-accuracy/10-02-SUMMARY.md`
</output>
