---
phase: 06-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/test_sensor.py
  - tests/test_coordinator.py
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "No test in test_sensor.py references the ml_status, cross_sensor_patterns, or ml_training_remaining sensor keys"
    - "No test in test_coordinator.py asserts that the stub keys (ml_status_stub, ml_training_stub, cross_sensor_stub) exist in coordinator data"
    - "The full test suite passes with make test"
  artifacts:
    - path: "tests/test_sensor.py"
      provides: "Sensor tests without ML stub test methods"
    - path: "tests/test_coordinator.py"
      provides: "Coordinator tests without stub key assertions"
  key_links:
    - from: "tests/test_coordinator.py test_async_update_data_returns_dict_with_all_keys"
      to: "coordinator.py _async_update_data return dict"
      via: "required_keys list assertion"
      pattern: "ml_status_stub|ml_training_stub|cross_sensor_stub"
---

<objective>
Update tests to match the production code changes made in Plan 01.

Purpose: Plan 01 removed the deprecated sensor entries and coordinator stub keys. The test files still assert on those removed names — they will fail until updated. This plan brings tests into alignment.
Output: Clean test suite with no references to the removed ML stubs.
</objective>

<execution_context>
@/Users/abourne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abourne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dead-code-removal/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key test methods to delete from test_sensor.py (use exact method names to locate and delete) -->

Class MLStatusSensorTests (or equivalent grouping) — methods to delete:
- test_ml_status_sensor_ready (line ~161)
- test_ml_status_sensor_trained_learning (line ~173)
- test_ml_status_sensor_learning (line ~185)
- test_ml_status_sensor_disabled (line ~197)
- test_ml_status_extra_attrs (line ~209)
- test_cross_sensor_patterns_sensor (line ~136)
- test_cross_sensor_patterns extra_attrs test (line ~151)
- test_ml_training_remaining_sensor (line ~383)
- test_ml_training_remaining_extra_attrs (line ~392)
- test_ml_status_stub_returns_removed_string_with_empty_data (line ~598)
- test_ml_status_stub_returns_removed_string_with_populated_data (line ~604)
- test_ml_status_stub_extra_attrs_has_deprecated_true (line ~614)
- test_ml_training_remaining_stub_returns_na_with_empty_data (line ~623)
- test_ml_training_remaining_stub_returns_na_with_populated_data (line ~629)
- test_ml_training_remaining_stub_extra_attrs_has_deprecated_true (line ~642)
- test_cross_sensor_stub_* methods near line ~653-671

Coordinator test changes (test_coordinator.py):
- test_async_update_data_returns_dict_with_all_keys (line ~136): remove "ml_status_stub", "ml_training_stub", "cross_sensor_stub" from required_keys list; keep all other keys including "ml_status" and "cross_sensor_patterns"
- test_async_update_data_ml_status_stub (line ~160): delete this entire test method
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove ML stub test methods from test_sensor.py</name>
  <files>tests/test_sensor.py</files>
  <action>
Delete all test methods in test_sensor.py that test the three removed sensor descriptions. Use grep to locate each by method name before deleting.

Methods to delete (search by def name):
- test_ml_status_sensor_ready
- test_ml_status_sensor_trained_learning
- test_ml_status_sensor_learning
- test_ml_status_sensor_disabled
- test_ml_status_extra_attrs
- test_cross_sensor_patterns_sensor
- Any adjacent test method testing cross_sensor_patterns extra attrs (the one at line ~151)
- test_ml_training_remaining_sensor
- test_ml_training_remaining_extra_attrs
- test_ml_status_stub_returns_removed_string_with_empty_data
- test_ml_status_stub_returns_removed_string_with_populated_data
- test_ml_status_stub_extra_attrs_has_deprecated_true
- test_ml_training_remaining_stub_returns_na_with_empty_data
- test_ml_training_remaining_stub_returns_na_with_populated_data
- test_ml_training_remaining_stub_extra_attrs_has_deprecated_true
- Any cross_sensor_stub test methods in the vicinity of line 653-671

For each method: delete from the `def test_...` line through (and including) the last line of that method body. Do not leave orphaned blank lines (leave at most one blank line between remaining methods).

Also check: if any of these deleted methods were in a dedicated test class (e.g., class TestMLStubs) with no remaining methods, delete the class definition too.

Do not delete tests for non-ML sensors. The test_sensor.py file should still test last_activity, activity_score, anomaly_detected, baseline_confidence, daily_activity_count, welfare_status, routine_progress, time_since_activity, entity_status_summary, statistical_training_remaining, and last_notification.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -m pytest tests/test_sensor.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>test_sensor.py contains no test methods referencing ml_status, cross_sensor_patterns, or ml_training_remaining sensor keys. pytest tests/test_sensor.py passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update coordinator tests to remove stub key assertions</name>
  <files>tests/test_coordinator.py</files>
  <action>
Make two targeted edits to test_coordinator.py:

**Edit 1 — test_async_update_data_returns_dict_with_all_keys:**
Find the `required_keys` list in this test method. Remove the three stub entries from the list:
- `"ml_status_stub"`
- `"ml_training_stub"`
- `"cross_sensor_stub"`

Keep all other entries in the list, including `"ml_status"` and `"cross_sensor_patterns"` (these keys still exist in coordinator data).

**Edit 2 — test_async_update_data_ml_status_stub:**
Delete the entire test method `test_async_update_data_ml_status_stub` (the method that asserts `data["ml_status_stub"] == "Removed in v1.1"` etc.). This method is now testing behaviour that no longer exists.

No other changes to test_coordinator.py.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -m pytest tests/test_coordinator.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>test_coordinator.py contains no references to ml_status_stub, ml_training_stub, or cross_sensor_stub. pytest tests/test_coordinator.py passes with no errors.</done>
</task>

</tasks>

<verification>
Full test suite passes:

```bash
cd /Users/abourne/Documents/source/behaviour-monitor && source venv/bin/activate && make test
```

All tests pass. No references to ml_status_stub, ml_training_stub, or cross_sensor_stub remain anywhere in the test directory:

```bash
grep -r "ml_status_stub\|ml_training_stub\|cross_sensor_stub" /Users/abourne/Documents/source/behaviour-monitor/tests/ && echo "FOUND — remove them" || echo "Clean"
```
</verification>

<success_criteria>
1. make test passes (all test files, no failures)
2. grep finds zero occurrences of ml_status_stub, ml_training_stub, cross_sensor_stub in tests/
3. test_sensor.py still tests the 11 remaining sensor descriptions (statistical_training_remaining is present, ML stubs are not)
4. test_coordinator.py required_keys list no longer includes the three stub keys
</success_criteria>

<output>
After completion, create `.planning/phases/06-dead-code-removal/06-02-SUMMARY.md`
</output>
