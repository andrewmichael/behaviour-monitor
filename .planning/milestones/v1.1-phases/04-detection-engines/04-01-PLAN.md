---
phase: 04-detection-engines
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/behaviour_monitor/alert_result.py
  - custom_components/behaviour_monitor/acute_detector.py
  - custom_components/behaviour_monitor/const.py
  - tests/test_acute_detector.py
autonomous: true
requirements:
  - ACUTE-01
  - ACUTE-02
  - ACUTE-03

must_haves:
  truths:
    - "Inactivity alert fires when elapsed time exceeds 3x the learned typical interval for the current time slot"
    - "Unusual-time alert fires when activity occurs in a slot with fewer than MIN_SLOT_OBSERVATIONS events"
    - "No alert fires from a single observation — 3 consecutive polling cycles of sustained evidence are required"
    - "Cycle counter resets to zero when the alerting condition clears, requiring fresh evidence to re-fire"
    - "Inactivity severity is LOW at 2-3x, MEDIUM at 3-5x, HIGH at 5x+ the threshold"
  artifacts:
    - path: "custom_components/behaviour_monitor/alert_result.py"
      provides: "AlertResult dataclass, AlertType enum, AlertSeverity enum"
      exports: ["AlertResult", "AlertType", "AlertSeverity"]
    - path: "custom_components/behaviour_monitor/acute_detector.py"
      provides: "AcuteDetector with check_inactivity and check_unusual_time methods"
      exports: ["AcuteDetector", "DEFAULT_INACTIVITY_MULTIPLIER", "SUSTAINED_EVIDENCE_CYCLES"]
    - path: "tests/test_acute_detector.py"
      provides: "Unit tests for acute detection logic"
      min_lines: 150
  key_links:
    - from: "custom_components/behaviour_monitor/acute_detector.py"
      to: "custom_components/behaviour_monitor/alert_result.py"
      via: "import AlertResult, AlertType, AlertSeverity"
      pattern: "from .alert_result import"
    - from: "custom_components/behaviour_monitor/acute_detector.py"
      to: "custom_components/behaviour_monitor/routine_model.py"
      via: "import EntityRoutine, MIN_SLOT_OBSERVATIONS"
      pattern: "from .routine_model import"
---

<objective>
Build the shared alert result types and the acute detector (inactivity + unusual-time) as pure-Python, HA-free components with full TDD coverage.

Purpose: Deliver the first detection engine that consumes the Phase 3 RoutineModel API, producing structured alert results that the Phase 5 coordinator will wire into sensors and notifications.

Output: `alert_result.py` (shared types for both detectors), `acute_detector.py` (inactivity + unusual-time detection), `test_acute_detector.py` (TDD tests), and CUSUM constants added to `const.py`.
</objective>

<execution_context>
@/Users/abourne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abourne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-detection-engines/04-CONTEXT.md
@.planning/phases/04-detection-engines/04-RESEARCH.md

@custom_components/behaviour_monitor/routine_model.py
@custom_components/behaviour_monitor/const.py
@tests/test_routine_model.py

<interfaces>
<!-- Key types from routine_model.py that this plan's code must consume -->

From custom_components/behaviour_monitor/routine_model.py:
```python
MIN_SLOT_OBSERVATIONS: int = 4

class ActivitySlot:
    event_times: deque[str]
    numeric_count: int
    @property
    def is_sufficient(self) -> bool: ...
    def expected_gap_seconds(self) -> float | None: ...

class EntityRoutine:
    entity_id: str
    is_binary: bool
    slots: list[ActivitySlot]  # 168 slots
    @staticmethod
    def slot_index(hour: int, dow: int) -> int: ...
    def expected_gap_seconds(self, hour: int, dow: int) -> float | None: ...
    def daily_activity_rate(self, target_date: date) -> int: ...
    def confidence(self, now: datetime) -> float: ...
```

From custom_components/behaviour_monitor/const.py:
```python
SENSITIVITY_LOW: Final = "low"
SENSITIVITY_MEDIUM: Final = "medium"
SENSITIVITY_HIGH: Final = "high"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create alert_result.py shared types and extend const.py with detection constants</name>
  <files>custom_components/behaviour_monitor/alert_result.py, custom_components/behaviour_monitor/const.py</files>
  <behavior>
    - AlertType enum has values: INACTIVITY, UNUSUAL_TIME, DRIFT (str enum for JSON serialization)
    - AlertSeverity enum has values: LOW, MEDIUM, HIGH (str enum)
    - AlertResult dataclass has fields: entity_id (str), alert_type (AlertType), severity (AlertSeverity), confidence (float), explanation (str), timestamp (str), details (dict)
    - AlertResult.to_dict() produces JSON-safe dict with enum .value strings
    - Round-trip: AlertResult -> to_dict -> verify keys match expected structure
    - const.py gains: DEFAULT_INACTIVITY_MULTIPLIER=3.0, SUSTAINED_EVIDENCE_CYCLES=3, MIN_EVIDENCE_DAYS=3, MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME=0.3
    - const.py gains: CUSUM_PARAMS dict mapping sensitivity levels to (k, h) tuples: high=(0.25, 2.0), medium=(0.5, 4.0), low=(1.0, 6.0)
  </behavior>
  <action>
Create `alert_result.py` as a pure-Python module (zero HA imports) with:
- `AlertType(str, Enum)` with INACTIVITY, UNUSUAL_TIME, DRIFT values
- `AlertSeverity(str, Enum)` with LOW, MEDIUM, HIGH values
- `AlertResult` dataclass with fields: entity_id, alert_type, severity, confidence, explanation, timestamp, details (dict with default_factory=dict)
- `to_dict()` method that converts enums to their .value strings for JSON serialization

Extend `const.py` with new detection constants (append after existing constants, before any class definitions):
- `DEFAULT_INACTIVITY_MULTIPLIER: Final = 3.0`
- `SUSTAINED_EVIDENCE_CYCLES: Final = 3`
- `MIN_EVIDENCE_DAYS: Final = 3`
- `MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME: Final = 0.3`
- `CUSUM_PARAMS: Final = {"high": (0.25, 2.0), "medium": (0.5, 4.0), "low": (1.0, 6.0)}`

Follow established pattern from routine_model.py: `from __future__ import annotations`, dataclass-based, stdlib only.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -c "from custom_components.behaviour_monitor.alert_result import AlertResult, AlertType, AlertSeverity; r = AlertResult(entity_id='test', alert_type=AlertType.INACTIVITY, severity=AlertSeverity.HIGH, confidence=0.9, explanation='test', timestamp='2024-01-01T00:00:00'); d = r.to_dict(); assert d['alert_type'] == 'inactivity'; assert d['severity'] == 'high'; print('OK')" && python -c "from custom_components.behaviour_monitor.const import DEFAULT_INACTIVITY_MULTIPLIER, CUSUM_PARAMS, SUSTAINED_EVIDENCE_CYCLES, MIN_EVIDENCE_DAYS, MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME; assert DEFAULT_INACTIVITY_MULTIPLIER == 3.0; assert CUSUM_PARAMS['medium'] == (0.5, 4.0); print('OK')"</automated>
  </verify>
  <done>alert_result.py exists with AlertResult, AlertType, AlertSeverity; const.py extended with CUSUM_PARAMS and detection defaults; all imports resolve; to_dict produces JSON-safe output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AcuteDetector with TDD — inactivity, unusual-time, and sustained evidence</name>
  <files>custom_components/behaviour_monitor/acute_detector.py, tests/test_acute_detector.py</files>
  <behavior>
    - check_inactivity returns None when expected_gap is None (sparse slot)
    - check_inactivity returns None when last_seen is None
    - check_inactivity returns None when elapsed < threshold (3x expected_gap)
    - check_inactivity returns None on cycles 1 and 2 even when threshold exceeded (ACUTE-03)
    - check_inactivity returns AlertResult on cycle 3 when threshold exceeded for 3 consecutive cycles
    - check_inactivity resets cycle counter when condition clears (elapsed < threshold), requiring 3 fresh cycles to re-fire
    - Inactivity severity: ratio 2.5x -> LOW, ratio 3.5x -> MEDIUM, ratio 6x -> HIGH
    - check_unusual_time returns None when slot.is_sufficient is True (normal slot)
    - check_unusual_time returns None when routine confidence < 0.3 (per Pitfall 6 in research — gates on minimum confidence to prevent firing during learning phase)
    - check_unusual_time returns AlertResult only after 3 consecutive cycles of unusual-time evidence
    - check_unusual_time resets counter when condition clears
    - AlertResult explanation contains human-readable context (entity_id, elapsed hours, typical interval)
    - Zero HA imports in acute_detector.py — grep confirms no "homeassistant" string
  </behavior>
  <action>
Create `tests/test_acute_detector.py` first (RED phase). Write tests for all behaviors listed above using mock EntityRoutine objects. Structure tests as:
- `test_inactivity_returns_none_sparse_slot` — expected_gap returns None
- `test_inactivity_returns_none_no_last_seen` — last_seen is None
- `test_inactivity_returns_none_below_threshold` — elapsed well under 3x
- `test_inactivity_requires_sustained_evidence` — cycles 1,2 return None; cycle 3 returns AlertResult
- `test_inactivity_counter_resets_on_condition_clear` — condition clears then returns: needs fresh 3 cycles
- `test_inactivity_severity_low` — ratio ~2.5x
- `test_inactivity_severity_medium` — ratio ~3.5x
- `test_inactivity_severity_high` — ratio ~6x
- `test_unusual_time_normal_slot_returns_none` — slot.is_sufficient True
- `test_unusual_time_low_confidence_returns_none` — confidence < 0.3
- `test_unusual_time_requires_sustained_evidence` — 3 cycles
- `test_unusual_time_counter_resets` — condition clear then returns
- `test_no_ha_imports` — grep acute_detector.py for "homeassistant"

Then create `acute_detector.py` (GREEN phase). Implementation follows research Pattern 2 closely:
- `AcuteDetector.__init__` accepts `inactivity_multiplier` (default from const.py DEFAULT_INACTIVITY_MULTIPLIER) and `sustained_cycles` (default from const.py SUSTAINED_EVIDENCE_CYCLES)
- `_inactivity_cycles` and `_unusual_time_cycles` dicts for per-entity counters
- `check_inactivity(entity_id, routine, now, last_seen)` -> AlertResult | None
- `check_unusual_time(entity_id, routine, now)` -> AlertResult | None — must gate on `routine.confidence(now) >= MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME` per research Pitfall 6
- `_inactivity_severity(ratio)` static method: <3.0 -> LOW, 3.0-5.0 -> MEDIUM, >=5.0 -> HIGH
- Every code path that determines "condition not met" MUST set counter to 0 before returning None (research Pitfall 2)

Use relative imports: `from .alert_result import ...`, `from .routine_model import EntityRoutine, MIN_SLOT_OBSERVATIONS`
Import constants from const.py: `from .const import DEFAULT_INACTIVITY_MULTIPLIER, SUSTAINED_EVIDENCE_CYCLES, MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME`
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -m pytest tests/test_acute_detector.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All test_acute_detector.py tests pass; AcuteDetector handles inactivity and unusual-time with sustained evidence (3 cycles); counter resets on condition clear; severity tiers correct; zero HA imports confirmed</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_acute_detector.py -x -v` — all tests pass
2. `python -c "from custom_components.behaviour_monitor.alert_result import AlertResult, AlertType, AlertSeverity"` — imports resolve
3. `grep -c "homeassistant" custom_components/behaviour_monitor/alert_result.py custom_components/behaviour_monitor/acute_detector.py` — returns 0 for both files
4. `make lint` — no linting errors in new files
</verification>

<success_criteria>
- AlertResult, AlertType, AlertSeverity types are importable and produce JSON-safe dicts
- AcuteDetector.check_inactivity fires when elapsed > 3x expected_gap for 3 consecutive cycles
- AcuteDetector.check_unusual_time fires when slot is sparse AND confidence >= 0.3 for 3 consecutive cycles
- No alert fires from fewer than 3 consecutive cycles of evidence
- Cycle counters reset when conditions clear
- Severity tiers: LOW (2-3x), MEDIUM (3-5x), HIGH (5x+) for inactivity
- Zero homeassistant imports in alert_result.py and acute_detector.py
- const.py extended with CUSUM_PARAMS and detection defaults
</success_criteria>

<output>
After completion, create `.planning/phases/04-detection-engines/04-01-SUMMARY.md`
</output>
