---
phase: 13-tier-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/behaviour_monitor/routine_model.py
  - tests/test_routine_model.py
autonomous: true
requirements: [CLASS-01, CLASS-02, CLASS-03, CLASS-05]

must_haves:
  truths:
    - "EntityRoutine with sufficient data (confidence >= 0.8) returns a valid ActivityTier from activity_tier property"
    - "EntityRoutine with insufficient data returns None from activity_tier property"
    - "Calling classify_tier() twice on the same calendar date does not recompute (once-per-day guard)"
    - "Tier changes are logged at DEBUG level with old and new tier values"
    - "Tier classification uses median daily event rate compared against TIER_BOUNDARY_HIGH (24) and TIER_BOUNDARY_LOW (4)"
  artifacts:
    - path: "custom_components/behaviour_monitor/routine_model.py"
      provides: "classify_tier method, activity_tier property, _compute_median_daily_rate helper"
      contains: "def classify_tier"
    - path: "tests/test_routine_model.py"
      provides: "Classification tests covering all tiers, gating, reclassification guard, logging"
      contains: "class TestTierClassification"
  key_links:
    - from: "custom_components/behaviour_monitor/routine_model.py"
      to: "custom_components/behaviour_monitor/const.py"
      via: "import ActivityTier, TIER_BOUNDARY_HIGH, TIER_BOUNDARY_LOW"
      pattern: "from .const import.*ActivityTier"
---

<objective>
Add tier classification to EntityRoutine: a `classify_tier()` method that computes median daily event rate from observed data, maps it to HIGH/MEDIUM/LOW using Phase 12 boundary constants, gates on learning confidence, and reclassifies at most once per day.

Purpose: This is the data-layer foundation for all tier-aware detection. Without classification, Phases 14-16 cannot branch on tier.
Output: `classify_tier()` method, `activity_tier` property, and comprehensive tests on EntityRoutine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-constants-and-utilities/12-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From custom_components/behaviour_monitor/const.py:
```python
class ActivityTier(Enum):
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"

TIER_BOUNDARY_HIGH: Final = 24   # events/day — >= this is HIGH
TIER_BOUNDARY_LOW: Final = 4    # events/day — <= this is LOW
```

From custom_components/behaviour_monitor/routine_model.py (EntityRoutine — existing):
```python
@dataclass
class EntityRoutine:
    entity_id: str
    is_binary: bool
    slots: list[ActivitySlot]
    history_window_days: int = 28
    first_observation: str | None = None

    def daily_activity_rate(self, target_date: date) -> int: ...
    def confidence(self, now: datetime) -> float: ...
    def to_dict(self) -> dict[str, Any]: ...
    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> "EntityRoutine": ...
```

From custom_components/behaviour_monitor/routine_model.py (RoutineModel — existing):
```python
class RoutineModel:
    def learning_status(self, now: datetime | None = None) -> str: ...
    # Returns "inactive", "learning", or "ready" (>= 0.8 confidence)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add classify_tier method and activity_tier property to EntityRoutine</name>
  <files>custom_components/behaviour_monitor/routine_model.py, tests/test_routine_model.py</files>
  <read_first>
    - custom_components/behaviour_monitor/routine_model.py (full file — current EntityRoutine class)
    - custom_components/behaviour_monitor/const.py (ActivityTier, TIER_BOUNDARY_HIGH, TIER_BOUNDARY_LOW)
    - tests/test_routine_model.py (existing test structure and helpers)
  </read_first>
  <behavior>
    - Test HIGH: entity with 30 events/day median across 5+ days, confidence >= 0.8 -> activity_tier == ActivityTier.HIGH
    - Test MEDIUM: entity with 10 events/day median -> activity_tier == ActivityTier.MEDIUM
    - Test LOW: entity with 2 events/day median -> activity_tier == ActivityTier.LOW
    - Test boundary HIGH exact: exactly 24 events/day -> ActivityTier.HIGH (>= boundary)
    - Test boundary LOW exact: exactly 4 events/day -> ActivityTier.LOW (<= boundary)
    - Test gating: confidence < 0.8 -> activity_tier returns None (per D-01)
    - Test no data: fresh entity -> activity_tier returns None
    - Test once-per-day guard: classify_tier() called twice same day -> second call is no-op (tier unchanged, no recomputation)
    - Test reclassification on new day: classify_tier() on day N sets tier, classify_tier() on day N+1 recomputes
    - Test tier change logging: when tier changes from MEDIUM to HIGH, a DEBUG log message is emitted containing old tier, new tier, and entity_id
    - Test no log on same tier: when tier stays the same after reclassification, no log message emitted
    - Test numeric entity: non-binary entity with no event_times -> activity_tier returns None (no binary data to classify)
  </behavior>
  <action>
    **Add imports** to routine_model.py at top:
    ```python
    import logging
    from .const import ActivityTier, TIER_BOUNDARY_HIGH, TIER_BOUNDARY_LOW
    ```
    Add module-level logger:
    ```python
    _LOGGER = logging.getLogger(__name__)
    ```

    **Add instance attributes** to EntityRoutine.__init__ (via post_init or default field):
    Since EntityRoutine is a dataclass, add these as fields with default values:
    ```python
    _activity_tier: ActivityTier | None = field(default=None, init=False, repr=False)
    _tier_classified_date: date | None = field(default=None, init=False, repr=False)
    ```

    **Add `_compute_median_daily_rate` private method** on EntityRoutine:
    - Scan ALL slots' event_times deques
    - Parse each timestamp, extract calendar date
    - Build dict[date, int] counting events per date
    - If no dates found, return None
    - Return float(median(list(date_counts.values()))) using statistics.median
    - This uses the full observation window, not a single day (per D-02: total events aggregated across deques, grouped by distinct days)

    **Add `classify_tier` method** on EntityRoutine:
    ```python
    def classify_tier(self, now: datetime) -> None:
    ```
    - Per D-01: Check `self.confidence(now) >= 0.8`. If not, set `self._activity_tier = None` and return.
    - Per CLASS-03: Check `self._tier_classified_date == now.date()`. If so, return (once-per-day guard).
    - Compute median_rate = self._compute_median_daily_rate(). If None, set tier to None and return.
    - Map rate to tier:
      - `median_rate >= TIER_BOUNDARY_HIGH` -> ActivityTier.HIGH
      - `median_rate <= TIER_BOUNDARY_LOW` -> ActivityTier.LOW
      - else -> ActivityTier.MEDIUM
    - Per CLASS-05: If old tier differs from new tier AND old tier is not None (skip logging on first classification), log at DEBUG:
      `_LOGGER.debug("Tier reclassified for %s: %s -> %s (median_rate=%.1f)", self.entity_id, old_tier.value, new_tier.value, median_rate)`
    - Set `self._activity_tier = new_tier`
    - Set `self._tier_classified_date = now.date()`

    **Add `activity_tier` property** on EntityRoutine:
    ```python
    @property
    def activity_tier(self) -> ActivityTier | None:
        return self._activity_tier
    ```
    Per D-04: Returns None for unclassified entities.

    **Tests** in tests/test_routine_model.py:
    - Add `from custom_components.behaviour_monitor.const import ActivityTier, TIER_BOUNDARY_HIGH, TIER_BOUNDARY_LOW` to imports
    - Create helper `_build_routine_with_rate(events_per_day: int, num_days: int)` that:
      - Creates an EntityRoutine with is_binary=True
      - Sets first_observation to 30+ days ago (ensures confidence=1.0)
      - Distributes `events_per_day * num_days` timestamps across `num_days` distinct calendar dates, spread across appropriate slots
      - Returns the routine and a `now` datetime
    - Add class `TestTierClassification` with all behavior tests listed above
    - For the logging test, use `caplog` pytest fixture with `logging.DEBUG` level
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -m pytest tests/test_routine_model.py -x -v -k "TestTierClassification" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def classify_tier" custom_components/behaviour_monitor/routine_model.py returns 1
    - grep -c "def _compute_median_daily_rate" custom_components/behaviour_monitor/routine_model.py returns 1
    - grep -c "def activity_tier" custom_components/behaviour_monitor/routine_model.py returns 1
    - grep -c "_activity_tier" custom_components/behaviour_monitor/routine_model.py returns at least 4 (field, property, classify_tier assignments)
    - grep -c "_tier_classified_date" custom_components/behaviour_monitor/routine_model.py returns at least 3 (field, guard check, assignment)
    - grep "TIER_BOUNDARY_HIGH" custom_components/behaviour_monitor/routine_model.py shows import usage
    - grep "_LOGGER.debug" custom_components/behaviour_monitor/routine_model.py shows tier change log line
    - grep -c "class TestTierClassification" tests/test_routine_model.py returns 1
    - python -m pytest tests/test_routine_model.py -x exits 0 (all tests pass, no regressions)
  </acceptance_criteria>
  <done>
    EntityRoutine has classify_tier() method, activity_tier property, and _compute_median_daily_rate helper.
    Classification gates on confidence >= 0.8, uses once-per-day guard, logs tier changes at DEBUG.
    All new and existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify lint, full test suite, and import correctness</name>
  <files>custom_components/behaviour_monitor/routine_model.py</files>
  <read_first>
    - custom_components/behaviour_monitor/routine_model.py (verify final state after Task 1)
  </read_first>
  <action>
    Run full lint and test suite to confirm no regressions:
    1. `make lint` — must pass with no errors (ruff + mypy)
    2. `make test` — full test suite must pass
    3. Verify routine_model.py has zero HA imports (pure Python constraint): grep for "from homeassistant" should return nothing
    4. Verify the `from .const import` line includes ActivityTier, TIER_BOUNDARY_HIGH, TIER_BOUNDARY_LOW
    5. If lint or type errors found, fix them (common issues: missing type annotation on _activity_tier field, dataclass field ordering with init=False fields)
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && make lint && make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - make lint exits 0
    - make test exits 0 with all tests passing
    - grep "from homeassistant" custom_components/behaviour_monitor/routine_model.py returns nothing (pure Python)
    - grep "from .const import" custom_components/behaviour_monitor/routine_model.py contains "ActivityTier"
  </acceptance_criteria>
  <done>
    Full lint and test suite pass. routine_model.py maintains pure-Python-only constraint. No regressions introduced.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_routine_model.py -x -v` — all tests pass including new TestTierClassification
2. `make lint` — no lint or type errors
3. `make test` — full suite passes, no regressions
4. `grep "def classify_tier" custom_components/behaviour_monitor/routine_model.py` — method exists
5. `grep "activity_tier" custom_components/behaviour_monitor/routine_model.py` — property exists
6. `grep "_LOGGER.debug" custom_components/behaviour_monitor/routine_model.py` — debug logging present
</verification>

<success_criteria>
- EntityRoutine.classify_tier(now) computes tier from median daily rate, gated on confidence >= 0.8
- EntityRoutine.activity_tier property returns ActivityTier | None
- Once-per-day reclassification guard prevents repeated computation on same calendar date
- Tier changes logged at DEBUG level with entity_id, old tier, new tier, and median rate
- Unclassified entities (insufficient data) return None — zero behavior change for them
- All 410+ existing tests still pass, 10+ new classification tests added
</success_criteria>

<output>
After completion, create `.planning/phases/13-tier-classification/13-01-SUMMARY.md`
</output>
