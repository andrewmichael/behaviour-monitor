---
phase: 06-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/behaviour_monitor/sensor.py
  - custom_components/behaviour_monitor/coordinator.py
  - custom_components/behaviour_monitor/const.py
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03

must_haves:
  truths:
    - "The three deprecated ML sensor descriptions (ml_status, cross_sensor_patterns, ml_training_remaining) are absent from SENSOR_DESCRIPTIONS in sensor.py"
    - "coordinator.py emits no stub keys (ml_status_stub, ml_training_stub, cross_sensor_stub) from either _async_update_data or _build_safe_defaults"
    - "const.py lines 128-163 (legacy constants block) are gone — SENSITIVITY_THRESHOLDS, ML_CONTAMINATION, MIN_CROSS_SENSOR_OCCURRENCES, ML_EMA_ALPHA, DEFAULT_ENABLE_ML, DEFAULT_RETRAIN_PERIOD, DEFAULT_ML_LEARNING_PERIOD, DEFAULT_CROSS_SENSOR_WINDOW, DEFAULT_TRACK_ATTRIBUTES, SEVERITY_THRESHOLDS are absent"
    - "const.py no longer defines CONF_SENSITIVITY, CONF_LEARNING_PERIOD, CONF_ENABLE_ML, CONF_RETRAIN_PERIOD, CONF_ML_LEARNING_PERIOD, CONF_CROSS_SENSOR_WINDOW, CONF_TRACK_ATTRIBUTES"
    - "All remaining constants (SENSITIVITY_LOW/MEDIUM/HIGH, SEVERITY_THRESHOLDS, CUSUM_PARAMS, SUSTAINED_EVIDENCE_CYCLES etc.) are untouched"
  artifacts:
    - path: "custom_components/behaviour_monitor/sensor.py"
      provides: "SENSOR_DESCRIPTIONS without ML stubs"
      contains: "statistical_training_remaining"
    - path: "custom_components/behaviour_monitor/coordinator.py"
      provides: "Update data dict without stub keys"
    - path: "custom_components/behaviour_monitor/const.py"
      provides: "Clean constants file without dead legacy block"
  key_links:
    - from: "sensor.py SENSOR_DESCRIPTIONS"
      to: "coordinator.py data dict"
      via: "value_fn and extra_attrs_fn lambdas"
      pattern: "data\\.get\\(\"ml_status"
---

<objective>
Remove all deprecated ML sensor stubs and dead constants from production source files.

Purpose: These three files contain code that has been dead since v1.1 — stub sensors that return hardcoded values, coordinator keys that serve no consumer, and a legacy constants block explicitly marked for removal. Clearing them reduces maintenance surface and prepares const.py for Phase 7 which will add new CONF_* entries.
Output: Three cleaned production files. Tests will be updated in Plan 02.
</objective>

<execution_context>
@/Users/abourne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abourne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove three deprecated sensor descriptions from sensor.py</name>
  <files>custom_components/behaviour_monitor/sensor.py</files>
  <action>
Delete the three BehaviourMonitorSensorDescription entries from SENSOR_DESCRIPTIONS (the tuple starting at line 46) that have keys "cross_sensor_patterns", "ml_status", and "ml_training_remaining". These are lines 98-106, 107-116, and 185-194 respectively in the current file. Leave all other descriptions untouched.

After removing the three entries, audit the imports at lines 22-34. Remove any imports that are no longer referenced in the file:
- ATTR_CROSS_SENSOR_PATTERNS (was only used in the cross_sensor_patterns extra_attrs_fn)
- ATTR_ML_STATUS (was only used in the baseline_confidence extra_attrs_fn on line 83 — check if it's still needed there before removing)

IMPORTANT: The `baseline_confidence` description at lines 74-86 uses ATTR_ML_STATUS in its extra_attrs_fn. That reference must be kept because baseline_confidence is NOT being removed. So do NOT remove the ATTR_ML_STATUS import.

ATTR_CROSS_SENSOR_PATTERNS was only used in the deleted cross_sensor_patterns description — remove that import.

Do not touch any other sensor descriptions or imports.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -c "from custom_components.behaviour_monitor.sensor import SENSOR_DESCRIPTIONS; keys = [s.key for s in SENSOR_DESCRIPTIONS]; assert 'ml_status' not in keys, f'ml_status still present'; assert 'cross_sensor_patterns' not in keys, f'cross_sensor_patterns still present'; assert 'ml_training_remaining' not in keys, f'ml_training_remaining still present'; assert 'statistical_training_remaining' in keys, f'stat training missing'; print('OK:', keys)"</automated>
  </verify>
  <done>SENSOR_DESCRIPTIONS contains no entries with keys ml_status, cross_sensor_patterns, or ml_training_remaining. The statistical_training_remaining entry is intact. Import list has no stale references.</done>
</task>

<task type="auto">
  <name>Task 2: Remove coordinator stub keys from coordinator.py</name>
  <files>custom_components/behaviour_monitor/coordinator.py</files>
  <action>
In `_async_update_data` (around line 286), remove the stub key assignments from the returned dict. The current line reads:
```
"ml_status_stub": "Removed in v1.1", "ml_training_stub": "N/A", "cross_sensor_stub": 0,
```
Delete this entire key-value group from the dict literal. The surrounding keys ("learning_status" and "baseline_confidence") must remain.

In `_build_safe_defaults` (around line 299), remove the same three stub keys:
```
"ml_status_stub": "Removed in v1.1", "ml_training_stub": "N/A", "cross_sensor_stub": 0,
```
Delete this group from the safe defaults dict as well.

Do not remove the `"ml_status": {"enabled": False}` or `"cross_sensor_patterns": []` entries — those feed the still-present baseline_confidence and other sensors. Only remove the three `_stub` suffixed keys.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -c "
import ast, sys
src = open('custom_components/behaviour_monitor/coordinator.py').read()
assert 'ml_status_stub' not in src, 'ml_status_stub still present'
assert 'ml_training_stub' not in src, 'ml_training_stub still present'
assert 'cross_sensor_stub' not in src, 'cross_sensor_stub still present'
assert 'ml_status' in src, 'ml_status dict was accidentally removed'
print('OK')
"</automated>
  </verify>
  <done>Neither _async_update_data nor _build_safe_defaults contains the three *_stub keys. The ml_status and cross_sensor_patterns keys (without _stub suffix) remain.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dead legacy constants and unused CONF_* constants from const.py</name>
  <files>custom_components/behaviour_monitor/const.py</files>
  <action>
Make two distinct removals from const.py:

**Removal 1 — Dead legacy constants block (lines 128-163):**
Delete from the comment `# ---------------------------------------------------------------------------` that precedes `# Legacy constants kept for backward compatibility...` through to the closing `# ---------------------------------------------------------------------------` line just before the `# Detection engine constants (v1.1)` section. This removes:
- The comment block header/footer
- SENSITIVITY_THRESHOLDS
- ML_CONTAMINATION
- MIN_CROSS_SENSOR_OCCURRENCES
- ML_EMA_ALPHA
- MIN_BUCKET_OBSERVATIONS
- MAX_VARIANCE_MULTIPLIER
- DEFAULT_SENSITIVITY
- DEFAULT_LEARNING_PERIOD
- DEFAULT_ENABLE_ML
- DEFAULT_RETRAIN_PERIOD
- DEFAULT_ML_LEARNING_PERIOD
- DEFAULT_CROSS_SENSOR_WINDOW
- DEFAULT_TRACK_ATTRIBUTES
- MIN_SAMPLES_FOR_ML
- SEVERITY_THRESHOLDS

IMPORTANT: Keep everything from `# Detection engine constants (v1.1)` onward — SUSTAINED_EVIDENCE_CYCLES, MIN_EVIDENCE_DAYS, MINIMUM_CONFIDENCE_FOR_UNUSUAL_TIME, CUSUM_PARAMS must remain.

**Removal 2 — Unused CONF_* constants (lines 9-16):**
Delete these seven lines from the Configuration keys block:
- `CONF_SENSITIVITY: Final = "sensitivity"`
- `CONF_LEARNING_PERIOD: Final = "learning_period"`
- `CONF_ENABLE_ML: Final = "enable_ml"`
- `CONF_RETRAIN_PERIOD: Final = "retrain_period"`
- `CONF_ML_LEARNING_PERIOD: Final = "ml_learning_period"`
- `CONF_CROSS_SENSOR_WINDOW: Final = "cross_sensor_window"`
- `CONF_TRACK_ATTRIBUTES: Final = "track_attributes"`

Keep all other CONF_* constants: CONF_MONITORED_ENTITIES, CONF_ENABLE_NOTIFICATIONS, CONF_NOTIFY_SERVICES, CONF_NOTIFICATION_COOLDOWN, CONF_MIN_NOTIFICATION_SEVERITY, CONF_HISTORY_WINDOW_DAYS, CONF_INACTIVITY_MULTIPLIER, CONF_DRIFT_SENSITIVITY.

Before deleting, confirm none of the seven CONF_* names appear in any other file (grep the codebase). If any are imported elsewhere, do not delete them and note which file uses them.
  </action>
  <verify>
    <automated>cd /Users/abourne/Documents/source/behaviour-monitor && python -c "
import custom_components.behaviour_monitor.const as c
# Removed names must not exist
for name in ['SENSITIVITY_THRESHOLDS','ML_CONTAMINATION','MIN_CROSS_SENSOR_OCCURRENCES',
             'ML_EMA_ALPHA','DEFAULT_ENABLE_ML','DEFAULT_RETRAIN_PERIOD',
             'DEFAULT_ML_LEARNING_PERIOD','DEFAULT_CROSS_SENSOR_WINDOW',
             'DEFAULT_TRACK_ATTRIBUTES','SEVERITY_THRESHOLDS',
             'CONF_SENSITIVITY','CONF_ENABLE_ML','CONF_RETRAIN_PERIOD',
             'CONF_ML_LEARNING_PERIOD','CONF_CROSS_SENSOR_WINDOW','CONF_TRACK_ATTRIBUTES']:
    assert not hasattr(c, name), f'{name} still exists in const'
# Required names must still exist
for name in ['CUSUM_PARAMS','SUSTAINED_EVIDENCE_CYCLES','CONF_MONITORED_ENTITIES',
             'CONF_ENABLE_NOTIFICATIONS','SENSITIVITY_LOW','SEVERITY_SIGNIFICANT']:
    assert hasattr(c, name), f'{name} was accidentally removed'
print('OK')
"</automated>
  </verify>
  <done>const.py no longer contains the 16 legacy constants or the 7 unused CONF_* names. The Detection engine constants block (CUSUM_PARAMS etc.) and all used CONF_* names remain. File parses without errors.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
cd /Users/abourne/Documents/source/behaviour-monitor && source venv/bin/activate && make lint
```

Lint must pass with no errors on the three modified files. If ruff or mypy flag anything, fix before proceeding to Plan 02.
</verification>

<success_criteria>
1. sensor.py SENSOR_DESCRIPTIONS tuple contains no entries with keys ml_status, cross_sensor_patterns, or ml_training_remaining
2. coordinator.py contains no occurrences of the strings "ml_status_stub", "ml_training_stub", or "cross_sensor_stub"
3. const.py contains none of the 16 removed legacy constants and none of the 7 removed CONF_* names
4. `python -c "import custom_components.behaviour_monitor.sensor; import custom_components.behaviour_monitor.coordinator; import custom_components.behaviour_monitor.const"` succeeds without ImportError
5. make lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-dead-code-removal/06-01-SUMMARY.md`
</output>
