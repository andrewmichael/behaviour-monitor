---
phase: 08-bootstrap-fix-and-closeout
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - VERS-01

must_haves:
  truths:
    - "MILESTONES.md has a v2.9 section with a git range placeholder that will be filled at milestone close"
    - "The v2.9 entry follows the same format as the v1.0 and v1.1 entries above it"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v2.9 milestone record with placeholder git range"
      contains: "v2.9 Housekeeping & Config"
  key_links: []
---

<objective>
Add the v2.9 milestone entry to MILESTONES.md, establishing the versioning convention for future milestones.

Purpose: VERS-01 requires that MILESTONES.md records the package version range for v2.9 when the milestone closes. The git range (first commit hash → last commit hash) is not known until the final commit of the milestone is made. This plan adds the entry now with a placeholder range so the convention is established — the executor will fill the actual git range from the real commit history at the time of execution.

Output: MILESTONES.md with a v2.9 section prepended above v1.1, matching the format of existing entries.
</objective>

<execution_context>
@/Users/abourne/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abourne/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Existing MILESTONES.md format to match. From .planning/MILESTONES.md: -->

```markdown
## v1.1 Detection Rebuild (Shipped: 2026-03-13)

**Phases completed:** 3 phases, 9 plans, 2 tasks

**Files modified:** 51 | **Lines of code:** 7,934 Python
**Git range:** `8dc94b2` (feat(03-01)) → `e118dcc` (docs(05-03))

**Key accomplishments:**
1. ...
2. ...

---
```

The v2.9 entry must follow this exact format. The git range is obtained by running:
- First commit of the milestone: `git log --oneline | grep -E "feat|fix|docs|refactor" | tail -1` (or look for the first v2.9 commit after v1.1 shipped on 2026-03-13)
- Last commit: `git log --oneline -1` (HEAD at time of execution)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add v2.9 entry to MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <action>
    Step 1 — Determine the actual git range for the v2.9 milestone. Run:
    ```bash
    git log --oneline
    ```
    Find the first commit that belongs to v2.9 work (Phase 6 onward — the first commit after `e118dcc` which closed v1.1). Also note the current HEAD hash which will be the last commit once this plan completes.

    Because this plan's own commit is not yet made, use `TBD` as the end hash — the convention is established by the structure, not the exact value. The executor MAY update with the real final hash if known at write time.

    Step 2 — Prepend the v2.9 section to MILESTONES.md above the existing v1.1 section. The new section must match the existing format exactly:

    ```markdown
    ## v2.9 Housekeeping & Config (Shipped: 2026-03-14)

    **Phases completed:** 3 phases, 6 plans

    **Files modified:** [count from git] | **Lines of code:** [count from wc -l]
    **Git range:** `[first-v2.9-commit-hash]` (fix(06-01)) → `[HEAD-hash]` (docs(08-02))

    **Key accomplishments:**
    1. Removed deprecated ML sensor stubs (ml_status, cross_sensor_patterns, ml_training_remaining) and dead constants block from const.py
    2. Removed unused CONF_* constant definitions (CONF_SENSITIVITY, CONF_ENABLE_ML, CONF_RETRAIN_PERIOD, and others)
    3. Exposed learning period (days) and attribute tracking toggle as user-configurable options in the HA config UI
    4. Added v4→v5 config migration so existing installs upgrade without manual reconfiguration
    5. Fixed missing post-bootstrap _save_data() call — routine model now survives an immediate restart after first load

    ---
    ```

    To get the file modified count and line count, run:
    ```bash
    git diff e118dcc..HEAD --name-only | wc -l
    find custom_components/behaviour_monitor tests -name "*.py" | xargs wc -l | tail -1
    ```

    Fill in the real values. Use the actual first v2.9 commit hash from `git log --oneline`.
  </action>
  <verify>
    <automated>grep -c "v2.9 Housekeeping" /Users/abourne/Documents/source/behaviour-monitor/.planning/MILESTONES.md</automated>
  </verify>
  <done>MILESTONES.md contains a v2.9 section at the top of the file with a real git range (first and last commit hashes), key accomplishments list, and file/line counts. The format matches the v1.1 and v1.0 entries.</done>
</task>

</tasks>

<verification>
`grep "v2.9" .planning/MILESTONES.md` returns the section header.
The file has three milestone entries total (v2.9, v1.1, v1.0) in reverse-chronological order.
</verification>

<success_criteria>
1. `.planning/MILESTONES.md` contains a `## v2.9 Housekeeping & Config` section
2. The section includes a real git range (actual commit hashes, not placeholders)
3. The section lists all 5 key accomplishments of the milestone
4. Format matches the existing v1.1 and v1.0 entries exactly
</success_criteria>

<output>
After completion, create `.planning/phases/08-bootstrap-fix-and-closeout/08-02-SUMMARY.md`
</output>
